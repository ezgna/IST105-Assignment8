# IST105 Assignment 8 – Django + MongoDB (Python 3.11)
Ryo Suzuki

- Python: 3.11.x
- Django: 5.1.x
- How to run (local):
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
django-admin --version